name: Ping URLs

on:
  schedule:
    - cron: "*/3 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping URLs
        run: |
          curl https://pdf-tools-dljh.onrender.com/
          
