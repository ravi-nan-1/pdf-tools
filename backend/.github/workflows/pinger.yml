name: Ping URLs

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping URLs
        run: |
          curl https://pdf-tools-dljh.onrender.com/
          curl https://algotrading-1-dluo.onrender.com/

